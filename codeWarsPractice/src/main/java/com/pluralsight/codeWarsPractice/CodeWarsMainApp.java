package com.pluralsight.codeWarsPractice;

import java.util.Arrays;

public class CodeWarsMainApp {
    public static void main(String[] args) {
       WeightSort weightSort = new WeightSort();
       String example = WeightSort.orderWeight("100 180 90 56 65 74 68 86 99");

       // put strings into an array list
        // comperator interface ( write comperator check weighting, if that doesnt work call comprable to compare as strings)
        // the job of a comperator is to compare them ( if they are the same they return 0)
        // collections.sort or streams interface

    }
}
